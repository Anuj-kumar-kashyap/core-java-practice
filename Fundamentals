History:
Java was developed by james gosling in 1992 at Sun Microsystems company.
Initially java has 210 classes.
For world wide java was comes with JDK version 1.0 in 1995.
Java gain popularity with its version 1.5 also known as JDK5 in 2004.
In 2010 java was owned by Oracle for 7.4b$.

Tokens: Smallest unit of program.
Identifiers: Name of class, variables, methods, etc.
In java we have some rules for naming.
1. Allowed characters: a-z, A-Z, 0-9, and two special characters ($ and _).
2. We cannot start any name with numeric digit. (Example: 1a, 4d, etc)
3. Case sensitive. (name, Name both are different)
4. Length limit of characters. (In java we can give name as long we want but more than 15 character are not recommended)
5. We cant use name of reserved words.(which are 53 in java)
6. We can use the name of predefined classes and Identifiers.

Reserved words(53): further divided into 50 keywords & 3 constant literals.
constant literals: true,false,null
50 keywords: 48 used keywords, 2 unused.
2 unused: goto & const
48 used keywords: Data types(8), Access modifiers(11), Control statements(11), Exception handling(6), Object related(4), Class related(6)
Data types(8): byte, short, int, long, float, double, character, boolean
Access modifiers(11): public, private, protected, final, abstract, static, strictfp(1.2v), synchronized, native, transient, volatile
Control statements(11): if, else, for, do, while, switch, case, break, continue, default, return
Exception handling(6): try, catch, throw, throws, finally, assert(1.4v)
Object related(4): new, instanceof, super, this
Class related(6): class, import, package, implements, extends, interface
(void & enum(1.5v)): These two doesn't belogs to any cattegory.

Data types: Primitive & non-primitive 
primitives are further divided into:
Integers(byte, short, int, long)
floating-point(float, double)
character, boolean
non-primitive: variables which are of type class.
Scanner s; (here s is a variable of class Scanner)
byte data type used widely in context with applicaton: In Java, the byte data type is very important and commonly used because most digital data is stored and transmitted in the form of bytes.

Now checking each data type:

Integer data types
1. byte(1B): we can use between -128 to 127.
public class First {

    public static void main(String[] args) {
        byte a = 128; // Type mismatch: cannot convert from int to byte
        System.out.println(a);
    }
}

2. short(2B): we can use between -32768 to 32767
In early time we use short data type over int because on that time we have 16-bit processor which is compatible to short(16bit).
 public class First {

    public static void main(String[] args) {
        short a = 32768; // Type mismatch: cannot convert from int to short
        System.out.println(a);
    }
} 
3. int(4B): we can use between -2147483647 to 2147483647
public class First {

    public static void main(String[] args) {
        int a = 2147483648; //The literal 2147483648 of type int is out of range
        System.out.println(a);
    }
}
4.long(8B): we can use between -2power63 to 2power63
public class First {

    public static void main(String[] args) {
        long a = 214748364l; // we have to put l or L at the end of the long value
        System.out.println(a);
    }
}

Floating data types

1. float(4B):  
public class First {

    public static void main(String[] args) {
        float a = 25.3f; //we have to put f at the end of float value otherwise it is consider as double & we can't assign double value in float type variable
        System.out.println(a);
    }
}
public class First {

    public static void main(String[] args) {
        float a = 25.3; //Type mismatch: cannot convert from double to float
        System.out.println(a);
    }
}
2. double(8B): 
public class First {

    public static void main(String[] args) {
        double a = 25.3; 
        System.out.println(a);
    }
}
byte -> short -> int -> long -> float -> double
char - > int -> long.. here we can store int into char also.
In this flow we can store the values of different data types in difff types. but vice versa is not true.

Difference between float & double:
FLOAT: 
4-5 values after decimal. (4.234)
Taking 4 byte space in memory
single precision(32bits)
Range from -3.4*10power(38) to 3.4*10power(38)
DOUBLE:
14-16 values after decimal.(4.23634634355)
Taking 8 byte space in memory
double precision(64bits)
Range from -1.7*10power(308) to 1.7*10power(308)

Character(2B): 
All programming languages that follow the ASCII table usually store each character in 1 byte (8 bits). That means one character takes 1 byte of memory.
However, Java does not follow ASCII. It follows Unicode, so in Java each character (char data type) uses 2 bytes (16 bits) of memory. 
Because of this, Java can support a much larger number of characters compared to ASCII-based languages.

BOOLEAN(1b): 
public class First {

    public static void main(String[] args) {
        boolean a = true;
        boolean b = True;//Invalid(True cannot be resolved to a variable)
        boolean b = "true";// Invalid(Type mismatch: cannot convert from String to boolean)
        boolean b = 0;//Invalid(Type mismatch: cannot convert from int to boolean)

    }
}
Primitive    Default value     Non-primitive(wrapper class)
  byte  ->         0      ->       Byte
  short ->         0      ->       Short
  int   ->         0      ->       Integer
  long  ->         0      ->       Long
  float ->        0.0f    ->       Float
double  ->        0.0d    ->       Double
   Char ->        space   ->       Character
boolean ->        faluse  ->       Boolean


Pure OOPS: all concepts of oops should be followed + using non-primitive data types only
programming language which obeys the above two conditions such programming languages are pure Object oriented.
java does not follow multiple inheritance & hybrid inheritance and using primitive data types like int, float, etc. So we can say that java is not pure object oriented.
Their are some language like 'Ruby' & 'small talk' which are pure object oriented. 







